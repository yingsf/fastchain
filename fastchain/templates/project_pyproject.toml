[project]
name = "{project_name}"
version = "0.1.0"
description = "FastChain project generated by fastchain init"
requires-python = ">=3.12"
dependencies = [
  "fastchain-cucc=={fastchain_version}",
  "uvicorn>=0.38.0",
]
